# Sentiment Analysis App

A simple Sentiment Analysis project using Python, scikit-learn, and Streamlit. 
Users can enter any comment, and the app will classify it as Positive (Good) or Negative (Bad) and provide an auto reply

## Features
- Analyze user comments in real-time
- Predict sentiment (Good/Bad)
- Auto-generate reply based on sentiment
- Simple Streamlit frontend

## Setup
1. Clone the repo
2. Create virtual environment using `uv`:
   ```bash
   uv venv create
   uv venv activate

3. Install dependencies:

pip install -r requirements.txt

4. Run the app:

streamlit run app.py
Dependencies

Python 3.11+

Streamlit

scikit-learn

pandas

numpy

nltk

joblib


---

✅ This is a **full working structure**.  
Once you run `streamlit run app.py` in uv environment, you can **enter comments** and see **sentiment + auto reply**.  

---

If you want, I can **upgrade this project next** with:

- Larger real dataset (IMDB / Twitter)  
- Better model (like Logistic Regression or BERT small for accuracy)  
- Stylish Streamlit interface with color for Good/Bad  

Do you want me to do that next?   
